%section.people
  %header
    %h2 People I Follow
  %table.table
    %thead
      %tr
        %th(width="30%")
        %th(width="20%") Videos in Queue
        %th(width="20%") Followers
        %th(width="30%") Unfollow
    %tbody
      - current_user.following_relationships.each do |relationship|
        - leader = relationship.user
        %tr
          %td
            %img(src="http://www.gravatar.com/avatar/#{Digest::MD5.hexdigest(leader.email_address.downcase)}?s=40")
            = link_to leader.full_name, leader
          %td.extra-padding= leader.queue_items.count
          %td.extra-padding= leader.followers.count
          %td.extra-padding
            = link_to relationship_path(relationship), method: :delete do
              %i.glyphicon.glyphicon-remove
